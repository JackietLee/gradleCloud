server:
  port: 8081
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/,http://peer2:1112/eureka/
spring:
  application:
    name: hello-service

management:
  server:
    port: 54002