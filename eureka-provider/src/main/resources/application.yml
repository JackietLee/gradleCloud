server:
  port: 80
eureka:
  instance:
    hostname: peer1
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/,http://peer2:1112/eureka/
spring:
  application:
    name: hello-service
